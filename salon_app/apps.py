from django.apps import AppConfig

# Contains metadata about the app (rarely needs edited)
class SalonAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'salon_app'
